[project]
name = "mtc-remote-control"
version = "0.1.0"
description = ""
authors = [
    {name = "Andre Mueller",email = "andre@kiwisound.de"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-osc (>=1.9.3,<2.0.0)"
]

[tool.poetry]
packages = [{include = "mtc_remote_control", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
